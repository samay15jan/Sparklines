services:
  tui-server:
    build: .
    container_name: tui-websocket

    user: root

    command: node server/server.js

    ports:
      - "8080:8080"

    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /tmp/sparklines-audio:/tmp/sparklines-audio:shared